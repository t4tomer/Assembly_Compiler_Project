#include <stdio.h>
#include "../Headers/FirstTransition.h"
#include "../Headers/Constants.h"
int main() {
   return firstTransition(TEMP_FILE); 
   /* TODO - need to be change to be as given file from args.*/
}